# Copy to .env.local for local development. Leave both empty to run in local-only mode (no sync).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Base path. "/" locally; the deploy workflow sets "/<repo>/" for GitHub Pages.
VITE_BASE=/
